import { Tool } from '@langchain/core/tools';
import { Context } from 'koishi';
import { ChatLunaPlugin } from 'koishi-plugin-chatluna/services/chat';
import { Config } from '..';
import { ChatLunaToolRunnable } from 'koishi-plugin-chatluna/llm-core/platform/types';
export declare function apply(ctx: Context, config: Config, plugin: ChatLunaPlugin): Promise<void>;
export declare class DrawTool extends Tool {
    private drawCommand;
    private readonly drawPrompt;
    name: string;
    constructor(drawCommand: string, drawPrompt: string);
    /** @ignore */
    _call(input: string, _: any, config: ChatLunaToolRunnable): Promise<string>;
    get description(): string;
    private rawDescription;
}
