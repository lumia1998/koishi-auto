var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
var __commonJS = (cb, mod) => function __require() {
  return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
};
var __export = (target, all) => {
  for (var name2 in all)
    __defProp(target, name2, { get: all[name2], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/locales/zh-CN.schema.yml
var require_zh_CN_schema = __commonJS({
  "src/locales/zh-CN.schema.yml"(exports2, module2) {
    module2.exports = { $inner: [{}, { $desc: "适配器配置", platform: "设置适配器平台名称。", loggerinfo: "日志调试模式" }] };
  }
});

// src/locales/en-US.schema.yml
var require_en_US_schema = __commonJS({
  "src/locales/en-US.schema.yml"(exports2, module2) {
    module2.exports = { $inner: [{}, { $desc: "Adapter Configuration", platform: "Adapter platform name", loggerinfo: "Logger debug mode" }] };
  }
});

// src/index.ts
var src_exports = {};
__export(src_exports, {
  Config: () => Config2,
  apply: () => apply,
  inject: () => inject,
  logger: () => logger2,
  name: () => name,
  reusable: () => reusable,
  usage: () => usage
});
module.exports = __toCommonJS(src_exports);
var import_koishi = require("koishi");
var import_chat = require("koishi-plugin-chatluna/services/chat");
var import_error2 = require("koishi-plugin-chatluna/utils/error");
var import_logger2 = require("koishi-plugin-chatluna/utils/logger");

// src/public-api-client.ts
var import_client = require("koishi-plugin-chatluna/llm-core/platform/client");
var import_model = require("koishi-plugin-chatluna/llm-core/platform/model");
var import_types = require("koishi-plugin-chatluna/llm-core/platform/types");
var import_error = require("koishi-plugin-chatluna/utils/error");

// src/public-api-requester.ts
var import_outputs = require("@langchain/core/outputs");
var import_api = require("koishi-plugin-chatluna/llm-core/platform/api");

// src/logger.ts
var logger;
var config;
function initializeLogger(newLogger, newConfig) {
  logger = newLogger;
  config = newConfig;
}
__name(initializeLogger, "initializeLogger");
function logInfo(...args) {
  if (config?.loggerinfo) {
    logger?.info?.(...args);
  }
}
__name(logInfo, "logInfo");

// src/public-api-requester.ts
var import_messages = require("@langchain/core/messages");
var sleep = /* @__PURE__ */ __name((ms) => new Promise((resolve) => setTimeout(resolve, ms)), "sleep");
var PublicApiRequester = class extends import_api.ModelRequester {
  constructor(ctx, _configPool, _pluginConfig, _plugin) {
    super(ctx, _configPool, _pluginConfig, _plugin);
    this._pluginConfig = _pluginConfig;
  }
  static {
    __name(this, "PublicApiRequester");
  }
  async *completionStreamInternal(params) {
    const internalParams = params;
    logInfo("Receive params from chatluna", JSON.stringify(params, null, 2));
    const requestUrl = "https://api.pearktrue.cn/api/aichat/";
    const messages = internalParams.input.map((message) => {
      let role;
      if (message instanceof import_messages.HumanMessage) {
        role = "user";
      } else if (message instanceof import_messages.SystemMessage) {
        role = "system";
      } else {
        role = "assistant";
      }
      return {
        role,
        content: message.content
      };
    });
    const requestBody = {
      messages,
      stream: false
    };
    let retries = 3;
    while (retries > 0) {
      try {
        logInfo(
          "Sending request to API:",
          requestUrl,
          JSON.stringify(requestBody, null, 2)
        );
        const response = await this.ctx.http.post(
          requestUrl,
          requestBody,
          {
            headers: { "Content-Type": "application/json" }
          }
        );
        logInfo(
          "Received response from API:",
          JSON.stringify(response, null, 2)
        );
        if (response.code !== 200 || !response.content) {
          throw new Error(
            `API returned an error or unexpected response: ${JSON.stringify(response)}`
          );
        }
        const responseText = response.content;
        yield new import_outputs.ChatGenerationChunk({
          text: responseText,
          message: new import_messages.AIMessageChunk({ content: responseText })
        });
        return;
      } catch (error) {
        this.logger.error(
          `Request failed, ${retries - 1} retries left.`,
          error
        );
        retries--;
        if (retries === 0) {
          const errorText = `Failed to get response from API after several retries: ${error.message}`;
          yield new import_outputs.ChatGenerationChunk({
            text: errorText,
            message: new import_messages.AIMessageChunk({ content: errorText })
          });
        } else {
          await sleep(1e3);
        }
      }
    }
  }
  get logger() {
    return logger2;
  }
  buildHeaders() {
    return {};
  }
};

// src/public-api-client.ts
var import_v1_shared_adapter = require("@chatluna/v1-shared-adapter");
var PublicApiClient = class extends import_client.PlatformModelAndEmbeddingsClient {
  constructor(ctx, _config, plugin) {
    super(ctx, plugin.platformConfigPool);
    this._config = _config;
    this.plugin = plugin;
    this.platform = _config.platform;
    this._requester = new PublicApiRequester(
      ctx,
      plugin.platformConfigPool,
      _config,
      plugin
    );
  }
  static {
    __name(this, "PublicApiClient");
  }
  platform = "another-public-api";
  _requester;
  async refreshModels(config2) {
    return [
      {
        name: "pearktrue",
        type: import_types.ModelType.llm,
        capabilities: [],
        maxTokens: 128e3
      }
    ];
  }
  _createModel(model) {
    const info = this._modelInfos[model];
    if (info == null) {
      throw new import_error.ChatLunaError(
        import_error.ChatLunaErrorCode.MODEL_NOT_FOUND,
        new Error(
          `The model ${model} is not found in the models: ${JSON.stringify(Object.keys(this._modelInfos))}`
        )
      );
    }
    if (info.type === import_types.ModelType.llm) {
      return new import_model.ChatLunaChatModel({
        modelInfo: info,
        requester: this._requester,
        model,
        maxTokenLimit: info.maxTokens,
        modelMaxContextSize: (0, import_v1_shared_adapter.getModelMaxContextSize)(info),
        timeout: this._config.timeout,
        maxRetries: this._config.maxRetries,
        llmType: "openai",
        isThinkModel: false
      });
    }
  }
};

// src/index.ts
var logger2;
var reusable = true;
var usage = `
<p>另一个<strong>零成本、快速体验Chatluna的适配器</strong>。</p>
<ul>
<li><strong>API来源：</strong> pearktrue</li>
<li>
<strong>接口地址：</strong>
<a href="https://api.pearktrue.cn/dashboard/detail/311" target="_blank" rel="noopener noreferrer">api.pearktrue.cn/dashboard</a>
</li>
</ul>
`;
function apply(ctx, config2) {
  logger2 = (0, import_logger2.createLogger)(ctx, "chatluna-another-public-api-adapter");
  initializeLogger(logger2, config2);
  ctx.on("ready", async () => {
    if (config2.platform == null || config2.platform.length < 1) {
      throw new import_error2.ChatLunaError(
        import_error2.ChatLunaErrorCode.UNKNOWN_ERROR,
        new Error("Cannot find any platform")
      );
    }
    const platform = config2.platform;
    const plugin = new import_chat.ChatLunaPlugin(ctx, config2, platform);
    plugin.parseConfig((config3) => {
      return [
        {
          apiKey: "any",
          apiEndpoint: "any",
          platform,
          chatLimit: config3.chatTimeLimit,
          timeout: config3.timeout,
          maxRetries: config3.maxRetries,
          concurrentMaxSize: config3.chatConcurrentMaxSize
        }
      ];
    });
    plugin.registerClient(() => new PublicApiClient(ctx, config2, plugin));
    await plugin.initClient();
  });
}
__name(apply, "apply");
var Config2 = import_koishi.Schema.intersect([
  import_chat.ChatLunaPlugin.Config,
  import_koishi.Schema.object({
    platform: import_koishi.Schema.string().default("public-api"),
    loggerinfo: import_koishi.Schema.boolean().default(false).description("日志调试模式").experimental()
  })
]).i18n({
  "zh-CN": require_zh_CN_schema(),
  "en-US": require_en_US_schema()
});
var inject = ["chatluna"];
var name = "chatluna-another-public-api-adapter";
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  Config,
  apply,
  inject,
  logger,
  name,
  reusable,
  usage
});
