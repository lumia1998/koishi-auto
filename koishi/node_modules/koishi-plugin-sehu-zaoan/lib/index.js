var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
var __export = (target, all) => {
  for (var name2 in all)
    __defProp(target, name2, { get: all[name2], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/index.ts
var src_exports = {};
__export(src_exports, {
  Config: () => Config,
  apply: () => apply,
  inject: () => inject,
  name: () => name
});
module.exports = __toCommonJS(src_exports);
var import_koishi = require("koishi");
var name = "sehu-zaoan";
var inject = {
  required: ["database", "cron"]
};
var Config = import_koishi.Schema.object({
  leaderboardLimit: import_koishi.Schema.number().default(10).description("排行榜显示的人数上限。"),
  enableAliases: import_koishi.Schema.boolean().default(true).description('是否启用 "早"、"晚" 等快捷指令别名。')
});
var greetings = {
  morning: {
    // 0-5点
    early: [
      "这么早就醒啦？夜猫子该睡觉了哦🦉",
      "星空为伴，但身体需要休息~快去补觉",
      "凌晨的勇士，世界还在沉睡中呢！",
      "哇！你比公鸡起得还早🐓",
      "晨光未现，你已醒来，记得补充睡眠哦",
      "这么早起床，你是要修仙吗？🧘",
      "月亮都还没下班呢，你就上班了？",
      "早起的鸟儿有虫吃，但你也太早了吧🐦",
      "凌晨的微风在说：再睡会儿吧~",
      "你的床在呼唤：主人快回来😴"
    ],
    // 6-9点
    normal: [
      "早安！美好的一天从此刻开始☀️",
      "晨光熹微，祝你有个元气满满的一天",
      "早上好！今天也要闪闪发光哦✨",
      "新的一天，新的希望，早安！",
      "太阳公公说：早起的你有好运降临",
      "晨风拂面，带来我的问候：早安！",
      "一日之计在于晨，加油！",
      "早安，今天也要做自己的小太阳🌞",
      "清晨的露珠折射出你的好心情💧",
      "早安！让微笑开启新的一天😊"
    ],
    // 10-11点
    lateMorning: [
      "上午好！太阳都晒屁股啦~",
      "早安...等等，这都快中午了吧？",
      "终于醒啦？早餐都变成早午餐了！",
      "懒虫起床！美好上午快过半啦🛏️",
      "早安！虽然时间有点晚，但祝福不减",
      "这是早安还是午安？总之祝你愉快",
      "早起的鸟儿有虫吃，晚起的你...有早午餐吃🍳",
      "上午的阳光在嘲笑你的懒觉😆",
      "早安！你成功避开了清晨的寒冷",
      "现在说早安，太阳都升到头顶了☀️"
    ],
    // 12点+
    late: [
      "这时间说早安？太阳都准备下山了🌇",
      "早安？您确定不是想说午安或晚安？",
      "您这'早起'可真是够独特的，直接跳过上午",
      "恭喜您成功错过整个早晨！🏆",
      "现在说早安，您这是穿越了吗？⏳",
      "早安！虽然时间不太对，但心意是真的❤️",
      "这时间起床，您昨晚是当夜猫子去了吧？🦉",
      "再晚点就可以直接说晚安了🌙",
      "您这起床时间，是倒时差回来的吗？✈️",
      "现在说早安，早餐店都关门了🍞"
    ]
  },
  night: {
    // 0-6点
    lateNight: [
      "凌晨的月光见证你的坚持，但身体需要休息啦~",
      "熬夜冠军非你莫属！快去充电吧🔋",
      "这时间说晚安？你确定不是早安？",
      "夜猫子模式启动！但请爱护你的肝🐱",
      "星星都困得眨眼了，你还在坚持什么？",
      "再熬夜的话，小心头发集体抗议哦！",
      "夜已深，你的细胞在求饶了！",
      "月亮说：快去睡觉，不然我要扣你工资了！",
      "熬夜一时爽，明天火葬场😭",
      "修仙虽好，可不要贪杯哦~"
    ],
    // 6-19点
    tooEarly: [
      "太阳公公还在加班呢，睡什么睡！",
      "现在睡觉？你是要倒时差当国际人吗？",
      "晚安？这时间说晚安也太离谱了吧！",
      "白天睡觉晚上嗨，你是蝙蝠侠吗🦇？",
      "这时间睡觉，你是要修仙吗？",
      "夕阳无限好，只是你太早🌇",
      "现在睡觉的话，半夜醒来可别哭~",
      "这么早就睡？夜生活还没开始呢！",
      "太阳还没下山呢，睡什么美容觉！",
      "现在睡觉，你是要错过整个白天吗？"
    ],
    // 19-24点
    normal: [
      "晚安，愿星辰守护你的梦境✨",
      "月色温柔，伴你一夜好眠🌙",
      "放下今日的疲惫，拥抱甜美的梦",
      "晚安，明天又是新的开始！",
      "让月光洗净烦恼，安心入眠吧",
      "盖好被子，做个美梦哦~",
      "愿你的梦中有星辰大海🌌",
      "晚安，愿黑夜温柔待你",
      "结束一天的忙碌，好好休息吧",
      "让被子封印你的疲惫，晚安！"
    ]
  }
};
function getRandomGreeting(type) {
  const now = /* @__PURE__ */ new Date();
  const hour = now.getHours();
  let pool;
  if (type === "morning") {
    const morningGreetings = greetings.morning;
    if (hour < 6) pool = morningGreetings.early;
    else if (hour < 10) pool = morningGreetings.normal;
    else if (hour < 12) pool = morningGreetings.lateMorning;
    else pool = morningGreetings.late;
  } else {
    const nightGreetings = greetings.night;
    if (hour >= 0 && hour < 6) pool = nightGreetings.lateNight;
    else if (hour >= 6 && hour < 19) pool = nightGreetings.tooEarly;
    else pool = nightGreetings.normal;
  }
  return pool[Math.floor(Math.random() * pool.length)];
}
__name(getRandomGreeting, "getRandomGreeting");
function getTodayDateString() {
  const now = /* @__PURE__ */ new Date();
  const year = now.toLocaleString("en-US", { timeZone: "Asia/Shanghai", year: "numeric" });
  const month = now.toLocaleString("en-US", { timeZone: "Asia/Shanghai", month: "2-digit" });
  const day = now.toLocaleString("en-US", { timeZone: "Asia/Shanghai", day: "2-digit" });
  return `${year}-${month}-${day}`;
}
__name(getTodayDateString, "getTodayDateString");
function apply(ctx, config) {
  ctx.model.extend("daily_greetings", {
    id: "unsigned",
    // 使用自增ID作为主键
    type: "string",
    pid: "string",
    date: "string",
    time: "timestamp",
    rank: "integer",
    name: "string"
  }, {
    autoInc: true
    // 开启主键自增
  });
  ctx.cron("0 1 * * *", async () => {
    const today = getTodayDateString();
    await ctx.database.remove("daily_greetings", {
      // 删除所有日期不是今天的数据
      date: { $ne: today }
    });
    ctx.logger.info("每日问候数据已清理。");
  });
  async function handleGreeting(session, type) {
    const today = getTodayDateString();
    const pid = session.uid;
    const existingRecord = await ctx.database.get("daily_greetings", { pid, date: today, type });
    if (existingRecord.length > 0) {
      const rank2 = existingRecord[0].rank;
      const greetingMsg2 = getRandomGreeting(type);
      const typeText2 = type === "morning" ? "起床" : "睡觉";
      await session.send((0, import_koishi.h)("quote", { id: session.messageId }) + `${greetingMsg2}
你今天已经是第 ${rank2} 位${typeText2}的人了哦！`);
      return;
    }
    const allTodayGreetings = await ctx.database.get("daily_greetings", { date: today, type });
    const count = allTodayGreetings.length;
    const rank = count + 1;
    await ctx.database.create("daily_greetings", {
      type,
      pid,
      date: today,
      time: /* @__PURE__ */ new Date(),
      rank,
      name: session.author.name || session.author.id
      // 优先使用昵称
    });
    let specialMessage = "";
    if (rank === 1) {
      specialMessage = type === "morning" ? "🏆哇！你是今天第一个起床的冠军！" : "🏆哇！你是今天第一个睡觉的乖宝宝！";
    }
    const greetingMsg = getRandomGreeting(type);
    const typeText = type === "morning" ? "起床" : "睡觉";
    await session.send((0, import_koishi.h)("quote", { id: session.messageId }) + `${greetingMsg}
你是今天第 ${rank} 位${typeText}的人。
${specialMessage}`);
  }
  __name(handleGreeting, "handleGreeting");
  ctx.command("早安", "发送早安问候").action(async ({ session }) => handleGreeting(session, "morning"));
  ctx.command("晚安", "发送晚安问候").action(async ({ session }) => handleGreeting(session, "night"));
  ctx.command("早安排行榜", "查看今天的起床先锋").action(async ({ session }) => {
    const today = getTodayDateString();
    const results = await ctx.database.get("daily_greetings", { date: today, type: "morning" }, {
      sort: { rank: "asc" },
      // 按排名升序
      limit: config.leaderboardLimit
      // 使用配置项
    });
    if (results.length === 0) {
      return "今天还没有人起床呢，快来抢占第一名吧！";
    }
    const list = results.map((item) => `第 ${item.rank} 名: ${item.name} (${item.time.toLocaleTimeString("zh-CN")})`);
    return (0, import_koishi.h)("quote", { id: session.messageId }) + `☀️ 今日早安排行榜 ☀️
` + list.join("\n");
  });
  ctx.command("晚安排行榜", "查看今天的睡觉标兵").action(async ({ session }) => {
    const today = getTodayDateString();
    const results = await ctx.database.get("daily_greetings", { date: today, type: "night" }, {
      sort: { rank: "asc" },
      // 按排名升序
      limit: config.leaderboardLimit
    });
    if (results.length === 0) {
      return "今天还没有人睡觉呢，愿你有个好梦！";
    }
    const list = results.map((item) => `第 ${item.rank} 名: ${item.name} (${item.time.toLocaleTimeString("zh-CN")})`);
    return (0, import_koishi.h)("quote", { id: session.messageId }) + `🌙 今日晚安排行榜 🌙
` + list.join("\n");
  });
  if (config.enableAliases) {
    const morningAliases = /* @__PURE__ */ new Set(["早", "早上好", "早啊", "早哦", "早安"]);
    const nightAliases = /* @__PURE__ */ new Set(["晚", "睡了", "睡觉了", "晚安"]);
    ctx.middleware(async (session, next) => {
      if (morningAliases.has(session.content)) {
        return session.execute("早安");
      }
      if (nightAliases.has(session.content)) {
        return session.execute("晚安");
      }
      return next();
    });
  }
}
__name(apply, "apply");
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  Config,
  apply,
  inject,
  name
});
