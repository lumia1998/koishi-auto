//#region src/lib.d.ts
/**
 * One Last Image 独立图像处理库
 * 可以独立于 Koishi 使用
 */
/**
 * 将图片转换为线稿效果
 * @param imageBuffer 图片的 Buffer 或 Uint8Array
 * @param config 可选配置对象
 * @returns 处理后的图片 Buffer
 */
declare function one_last_image(imageBuffer: Buffer | Uint8Array, config?: Record<string, any>): Buffer;
//#endregion
export { one_last_image };