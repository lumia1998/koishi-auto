var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
var __commonJS = (cb, mod) => function __require() {
  return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
};
var __export = (target, all) => {
  for (var name2 in all)
    __defProp(target, name2, { get: all[name2], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// locales/zh-CN.yml
var require_zh_CN = __commonJS({
  "locales/zh-CN.yml"(exports, module2) {
    module2.exports = { commands: { choose: { description: "从给定的选项中选择一个", messages: { "no-delimiter": "", "same-choices": "这几个选项都是一样的！", chosen: "你选择了 {0}" } } } };
  }
});

// src/index.ts
var src_exports = {};
__export(src_exports, {
  Config: () => Config,
  apply: () => apply,
  name: () => name,
  usage: () => usage
});
module.exports = __toCommonJS(src_exports);
var import_koishi = require("koishi");
var name = "choose";
var Config = import_koishi.z.object({
  delimiters: import_koishi.z.array(import_koishi.z.string()).default([" ", "/", ",", ";", "，", "、", "；", "还是", "或(?:者|是)?"]).description("分隔符正则表达式列表，请注意 split 方法会把捕获组里的内容追加到分割出的数组里，因此使用非捕获组")
});
var usage = `本插件没有注册中间件，推荐和 dialogue 插件同时使用，这里提供示范：
\`\`\`
# -x -E ^选择(.+) $(choose $1)
\`\`\`
`;
function apply(ctx, config) {
  const logger = ctx.logger("choose");
  ctx.i18n.define("zh-CN", require_zh_CN());
  const delimiters = new RegExp(`(?:${config.delimiters.join("|")})`, "u");
  logger.debug("delimiters: ", delimiters);
  ctx.command("choose <input:text>").action(({ session }, input) => {
    if (!delimiters.test(input))
      return session.text(".no-delimiter");
    const choices = input.split(delimiters).map((choice) => choice?.trim()).filter(Boolean);
    logger.debug("choices: ", choices);
    const num = new Set(choices).size;
    if (num < 2)
      return session.text(".same-choices");
    const chosen = choices[Math.floor(Math.random() * choices.length)];
    return session.text(".chosen", [chosen]);
  });
}
__name(apply, "apply");
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  Config,
  apply,
  name,
  usage
});
