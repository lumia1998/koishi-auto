import { Context, Service } from 'koishi';
import { Config } from '../index';
import { GoldenQuote, SummaryTopic, UserPersonaProfile, UserStats, UserTitle } from '../types';
export declare class LLMService extends Service {
    config: Config;
    static readonly inject: string[];
    private model;
    constructor(ctx: Context, config: Config);
    private loadModel;
    private _callLLM;
    summarizeTopics(messagesText: string): Promise<SummaryTopic[]>;
    analyzeUserTitles(users: UserStats[]): Promise<UserTitle[]>;
    analyzeGoldenQuotes(messagesText: string, maxQuotes: number): Promise<GoldenQuote[]>;
    analyzeUserPersona(userId: string, username: string, roles: string[], recentMessages: string, previousAnalysis?: string): Promise<UserPersonaProfile | null>;
}
declare module 'koishi' {
    interface Context {
        chatluna_group_analysis_llm: LLMService;
    }
}
